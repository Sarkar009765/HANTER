export function ResponsePanel() {
    return (
        <div className="rounded-lg bg-bg-secondary border border-border-subtle p-4">
            <p className="text-sm text-text-muted">Response panel - select an agent or send a command to see results.</p>
        </div>
    );
}
